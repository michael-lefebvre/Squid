
;(function (window, undefined) 
{
  'use strict';

  // Prepare our Variables
  var document      = window.document
    , $             = window.jQuery
    , $document     = $( document )
    , $window       = $( window )
    , $body         = $( document.body )
    , AppRouter     = false

